# resume.zhilin.club - HTTP endpoint used for ACME and pre-certificate access.
server {
    listen 80;
    server_name resume.zhilin.club;

    root /usr/share/nginx/html/resume;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
